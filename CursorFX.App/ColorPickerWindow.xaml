<Window x:Class="CursorFX.App.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fluxor · Color Picker"
        Icon="pack://application:,,,/Assets/FluxorIco.ico"
        Width="460"
        Height="560"
        MinWidth="420"
        MinHeight="460"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBackgroundBrush}">
    <DockPanel Margin="20">
        <Border DockPanel.Dock="Bottom"
                Margin="0,16,0,0"
                Padding="0,12,0,0"
                BorderBrush="#1E293B"
                BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="OnCancelClick" />
                <Button Content="OK" Click="OnAcceptClick" />
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock FontSize="22" FontWeight="SemiBold" Text="{Binding PickerTitle}" />
                <Border Margin="0,16,0,16"
                        Height="64"
                        CornerRadius="12"
                        BorderBrush="#334155"
                        BorderThickness="1"
                        Background="{Binding PreviewBrush}" />

                <TextBlock Text="HEX" />
                <TextBox Text="{Binding HexColor, UpdateSourceTrigger=PropertyChanged}" />

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="A" Margin="0,0,12,0" VerticalAlignment="Center" />
                    <Slider Grid.Column="1" Minimum="0" Maximum="255" Value="{Binding Alpha}" />
                    <TextBlock Grid.Column="2" Text="{Binding Alpha}" VerticalAlignment="Center" HorizontalAlignment="Right" />

                    <TextBlock Grid.Row="1" Text="R" Margin="0,12,12,0" VerticalAlignment="Center" />
                    <Slider Grid.Row="1" Grid.Column="1" Margin="0,12,0,0" Minimum="0" Maximum="255" Value="{Binding Red}" />
                    <TextBlock Grid.Row="1" Grid.Column="2" Margin="0,12,0,0" Text="{Binding Red}" VerticalAlignment="Center" HorizontalAlignment="Right" />

                    <TextBlock Grid.Row="2" Text="G" Margin="0,12,12,0" VerticalAlignment="Center" />
                    <Slider Grid.Row="2" Grid.Column="1" Margin="0,12,0,0" Minimum="0" Maximum="255" Value="{Binding Green}" />
                    <TextBlock Grid.Row="2" Grid.Column="2" Margin="0,12,0,0" Text="{Binding Green}" VerticalAlignment="Center" HorizontalAlignment="Right" />

                    <TextBlock Grid.Row="3" Text="B" Margin="0,12,12,0" VerticalAlignment="Center" />
                    <Slider Grid.Row="3" Grid.Column="1" Margin="0,12,0,0" Minimum="0" Maximum="255" Value="{Binding Blue}" />
                    <TextBlock Grid.Row="3" Grid.Column="2" Margin="0,12,0,0" Text="{Binding Blue}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
